// N pair (a,b) we can choose a or b from pair such that sum of all number is equal to m
// if we choose 'a' then add '0' into string or if choose 'b' then add '1' into string.
// return {"YES","ans_String"} if it posiible to get m or {"NO"} if it is not possibel to do

+


#include<bits/stdc++.h>
using namespace std;

typedef long long ll;

void init_code(){
	#ifndef ONLINE_JUDGE
	freopen("F:/CP/input.txt", "r", stdin);
	freopen("F:/CP/output.txt", "w", stdout);
	#endif 
}

int main(){
	init_code();
	
	return 0;
}
